# japanese-study-grammar 운영 매뉴얼

이 문서는 "무슨 일이 있었는가"가 아니라 **"문제가 생기면 지금부터 뭘 해야 하는가"**를
정리한 실전 매뉴얼이다. 설계 배경이나 왜 이렇게 만들었는지는 `README.md`를 본다.

---

## 1. 이번 개선으로 달라진 것

### 이전 (수동 대응)

```
발송 실패 → 사용자가 GitHub Actions 접속 → 로그 펼치기 → 원인 찾기
→ Claude에게 캡처해서 전달 → 진단·수정 → 파일 다운로드 → 웹 업로드
```

### 지금 (자동 진단 + PR 기반 반영)

```
발송 실패 → 실패 알림 메일 하나로 원인 진단에 필요한 정보가 다 옴
→ 그 메일 내용을 Claude에게 그대로 전달 → 진단·수정
→ Claude Code 세션에서 수정 후 PR 생성
```

### 구체적으로 무엇이 바뀌었나

| 항목 | 이전 | 지금 |
|---|---|---|
| 실패 표시 | 조용히 종료(초록 체크로 위장) | `exit(1)`로 워크플로가 빨간 X로 표시됨 |
| 실패 원인 확인 | GitHub Actions 로그를 직접 열어야 함 | 실패 메일 본문에 시도별 진단이 다 담김 |
| 반복 문제 인지 | 매번 새로 판단 | 최근 5회 중 3회 이상 실패한 문형은 메일에 자동 경고 |
| 코드 반영 | 파일 다운로드 → 웹 업로드 | Claude Code 세션에서 수정 후 PR |
| 문장 수 미달/초과 | 검증 범위가 느슨해서 8~9문장도 통과된 적 있음 | 정확히 10~20문장만 통과 + 프롬프트에 목표치·자체 점검 지시 강화 |
| そうだ(伝聞/様態) 구분 | 문자열만 보고 구분 못 함 | Janome 형태소 분석으로 활용형까지 확인 |
| 위험 표현(あげく 등 12개) | 검증 없음 | 8개는 구조 검증, 나머지는 프롬프트 지침만(한계로 남음) |

---

## 2. 문제 발생 시 대응 절차

### 2-1. 실패 메일을 받았을 때 (가장 흔한 경우)

1. 운영자 Gmail 계정(`GMAIL_ADDRESS`)에서 제목이 `[운영 알림] 표현독해 발송 실패`인 메일을 연다
2. 메일 본문 전체를 복사한다 (반복 경고, 시도별 진단 다 포함되어 있음)
3. Claude와의 대화에서 그대로 붙여넣고 "확인 및 검토" 요청
4. Claude가 원인을 진단하고 수정 방향을 제안 — **여기서 반드시 승인 여부를 확인받는다**
   (한 번에 다 고치려 하지 말고, 원인이 여러 개면 하나씩 검증 후 진행)
5. 승인하면 Claude가 로컬에서 코드 수정 → 컴파일 확인 → 모의 테스트로 검증
6. Claude Code 세션에서 커밋 후 PR을 생성한다
7. GitHub Actions에서 **Run workflow**로 같은 카테고리를 강제 지정해 재실행 확인

### 2-2. 지문 내용이 이상하다고 느껴질 때 (실패는 아니지만 품질 문제)

발송은 됐지만 문장이 어색하거나 문법이 의심스러운 경우:

1. 받은 지문 원문을 그대로 Claude에게 붙여넣고 "이 문장을 읽고 검수" 요청
2. Claude가 문법적 정확성과 자연스러움을 검토
3. 실제 결함이 확인되면, **해당 문형 하나에 한해서만** 지침 보강 여부를 논의
   (89개 전체에 미리 지침을 도배하지 않는다 — 부작용이 더 크다는 게 이미 확인됨)
4. 수정 후 Claude Code 세션에서 PR 생성, Actions에서 같은 카테고리로 재실행해 확인

### 2-3. 워크플로 자체가 몇 주간 계속 실패할 때 (구조적 문제 의심)

1. `failure_history.json`을 GitHub에서 직접 열어 최근 실패 패턴을 확인
2. 특정 카테고리(예: N1문어체)에서만 반복된다면, 그 카테고리의 문형 조합 자체를 재검토
3. `PATTERNS_PER_DAY`(현재 5)를 줄이는 것도 검토 대상 — README의 "문형 조합 충돌 방지"
   섹션 참고

### 2-4. 확인 대기 중인 항목

> **다음 월요일 실행**: `send_weekly_shadow_report()`(2-5절) 발동 검증이 아직
> 남아 있다. 2026-09-14(월) 회차가 5시간41분 지연돼 화요일로 밀리는 바람에
> 월요일 조건이 성립하지 않아 발동하지 못했다(그 밀림 자체는 `_send_date()`
> 도입으로 해소). `shadow_review.json`엔 2026-09-10 `ばかりに`, 2026-09-15
> `そうだ(様態)` 등 `human_label: null`인 불일치가 쌓여 있어, 정상 발동하면
> 메일에 포함되어야 한다. 확인되면 이 문단은 삭제.

> **다음 부사(월) 회차**: PR #12의 `てしまう` 활용형 매칭 수정이 아직 실전
> 검증되지 않았다. 2026-09-14 회차가 화요일 '인용'으로 밀리면서 부사
> 카테고리가 돌지 않았기 때문이다. `かえって〜てしまう`가 선택되면 정상
> 통과해야 한다. 확인되면 이 문단은 삭제.

> **다음 지연 발생 시**: `_send_date()`가 회차 날짜를 잡아주는지 확인한다.
> `run_log.txt`의 `[카테고리] <날짜> → <카테고리>` 줄이, 실행이 늦게 떴어도
> **예정 요일의 카테고리**를 가리켜야 한다. 지연 여부는 Actions 실행 시각으로
> 판단한다(11:00 UTC 예정). 확인되면 이 문단은 삭제.

> **2026-09-19(토)**: PR #13으로 cron을 `0 11 * * 1-5`로 한정한 뒤 첫 토요일.
> Actions에 schedule 트리거 실행이 **없어야** 정상이다. cron 요일 필드는 기본
> 브랜치 기준으로만 동작해서 머지 전에는 실증할 방법이 없고, 평일에는 변경 전후가
> 똑같이 실행되므로 구별이 안 된다. 즉 이날이 유일한 검증 시점이다. 확인되면
> 이 문단은 삭제.

### 2-5. 주간 판정 불일치 리뷰

매주 월요일, `_EXTRA_CHECKS`(코드 검증)와 `judge_naturalness()`(LLM 판정) 결과가
갈린 사례가 있으면 `[운영 알림] 판정 불일치 주간 리뷰` 메일이 온다(없으면 메일 자체가
안 온다).

1. 메일에 나열된 각 사례(날짜·카테고리·문형·코드 판정·LLM 판정·지문 일부)를 읽는다
2. GitHub에서 `shadow_review.json`을 열어, 해당 항목의 `human_label`을 다음 중
   하나로 채운다:
   - `"code_wrong"` — 코드 검증이 틀렸다(지문은 문법적으로 맞는데 코드가 실격시킴)
   - `"judge_wrong"` — LLM 판정이 틀렸다(지문이 실제로 틀렸는데 LLM이 통과시킴, 또는
     그 반대)
   - `"ambiguous"` — 사람이 봐도 애매해서 판단 보류
   라벨 입력 UI는 없다 — 파일을 직접 고치거나, 메일 내용을 다음 claude.ai 대화에
   붙여넣고 "이 리포트 라벨링해줘"로 위임한다
3. 라벨링된 데이터가 문형당 10건 이상 쌓이면, README "그림자 비교" 절의 전환
   기준(LLM 오탐률 < 코드 오탐률, LLM 누락률 5% 미만)을 사람이 직접 확인해 그
   문형만 개별적으로 `_EXTRA_CHECKS`에서 뺄지 판단한다 — 자동 전환 코드는 없다

---

## 3. 자주 나오는 실패 사유와 1차 해석

| 실패 사유 문구 | 의미 | 우선 확인할 것 |
|---|---|---|
| `문장 수 N개 — 범위(10~20) 벗어남` | Gemini가 지시한 분량을 못 지킴 | 반복되면 프롬프트 목표치 재조정 검토 |
| `생성 중간에 잘림(마지막 문장이 완성되지 않음)` | 응답이 `max_output_tokens` 한도에 걸려 도중에 끊김 | 반복되면 `max_output_tokens`(현재 2200)를 추가로 올릴 신호 |
| `문형 누락: XX` | Gemini가 지정한 문형을 아예 안 씀 | 문형 조합 자체가 어려운 조합인지 확인. `らしい`는 형태소 분석까지 거치므로, 「素晴らしい」처럼 꼬리만 같은 어휘는 여기서 누락으로 잡힌다 |
| `구조 조건 미충족: XX` | 위험 문형 12개 중 하나가 잘못 쓰임 | 검증기 오탐일 수도, Gemini가 실제로 틀렸을 수도 있다 — 원문을 먼저 읽고 어느 쪽인지 판별한 뒤 지침 보강 여부를 정한다(09-10 `あげく` 사례는 후자: 긍정적 결과에 써서 실제 문법 오류였음) |
| `자연스러움 판정 실패: XX` | 문형 XX가 문법적으로 잘못 쓰임(LLM 판정) | `[판정-실격]` 줄에서 근거 확인. 오탐인지 실제 오류인지는 원문을 읽고 사람이 최종 판단 |
| `Gemini 출력 파싱 실패` | `---` 구분자를 안 지킴 | Gemini API 자체 이슈일 수 있음, 며칠 지켜보고 반복되면 프롬프트 포맷 지시 강화 |

---

## 4. 손대면 안 되는 것 (이미 검증된 함정)

- **89개 문형에 지침을 한꺼번에 추가하지 않는다** — N1문어체의 격언체 문형(べからず 등)에는
  오히려 역효과. 실제 문제가 확인된 문형 하나씩만 개별 대응한다.
- **검증 창(window) 크기를 무작정 넓히지 않는다** — `ない`처럼 흔한 단어 기반 검증은
  창을 넓히면 검증 자체가 무의미해진다.
- **결과 강제 문형(あげく·ばかりに·ないことには·なくしては·いかんによって) 5개는 하루
  최대 1개만 뽑히도록 고정되어 있다** — 이 로직을 건드리면 문장 수 실패율이 다시
  올라갈 수 있다(실제로 겪었던 문제).
- **Claude API나 유료 AI로 문법 재검증하는 방식은 이미 검토 후 보류했다** — 무료 운영
  전제와 안 맞는다. 다시 검토하려면 이 전제 자체를 먼저 재논의해야 한다.
- **`_normalize()`의 치환 순서를 바꾸지 않는다** — `故に`→`ゆえに` 정규식은 반드시
  `_KANJI_TO_KANA`(事→こと 포함) 치환보다 먼저 실행해야 한다. 순서를 바꾸면 `事故に`/
  `縁故に`의 `事`/`縁`이 먼저 `こと`/그대로 남은 상태에서 정규식이 돌지 않고, `事`가
  `こと`로 먼저 치환되어 룩비하인드가 무력화되면서 `事故に`가 `ゆえに`로 오탐 치환된다.
- **정규화 테이블에 어휘를 추가할 때 오탐 범위를 먼저 확인한다** — `故` 한 글자처럼 흔한
  글자를 무작정 치환 대상에 넣으면 `事故`·`縁故`·`故郷` 같은 무관한 단어 안에서도 우연히
  매치될 수 있다. 추가 전에 어떤 기존 단어와 겹치는지 확인하고, 필요하면 정규식
  룩비하인드로 범위를 제한한다.
- **쿨다운 우회 임계값에는 여유 마진이 필요하다** — 후보 수가 PATTERNS_PER_DAY와
  같거나 조금 큰 상태(예: 6개로 5개 선택)에서는 조합이 몇 가지로 고정되어
  재시도 4회가 무의미해진다. 임계값을 PATTERNS_PER_DAY로 되돌리지 않는다.
- **수동 실행으로 쿨다운 이력을 오염시키지 않는다** — FORCE_CATEGORY 테스트를
  같은 날 여러 번 돌리면 다음 실행의 후보 풀이 말라붙는다(실제로 겪었던 문제).
- **판정 프롬프트에서 "자연스러운가"를 묻지 않는다** — "틀렸는가"만 물어야 한다.
- **판정 모델을 생성 모델과 분리한다** — 같은 모델이 자기 글을 스스로 판정하면
  방금 만든 오류를 못 보는 경향이 있다.
- **판정은 기존 `_EXTRA_CHECKS`를 대체하지 않는다** — 재현율이 검증된 것을
  미검증 방법으로 교체하지 않는다. 추가로만 쌓는다.
- **`_EXTRA_CHECKS`의 어휘 목록은 실제 생성분에 등장한 어휘만 추가한다** — 가상의
  예문으로 예방적 확장을 하지 않는다. 코드 검증의 통과 여부와는 무관하다(통과했어도
  다른 의미로 흔히 안 쓰이는 안전한 어휘면 추가 대상이다 — 落ち込む 사례 참고).
  다만 이렇게 발견할 때마다 수동으로 추가하는 방식 자체가 무한 반복이라, 그림자
  비교 체계(아래)로 점진적으로 대체하는 중이다.
- **판정은 기존 코드 검증을 대체하지 않되, 그림자 비교로 실측 데이터가 쌓이면
  문형별로 개별 전환을 검토한다** — 5개(또는 11개) 문형을 일괄 전환하지 않는다.
  문형당 라벨링된 불일치 10건 이상 + LLM 오탐률이 코드 오탐률보다 낮고 + LLM
  누락률 5% 미만, 이 세 조건을 개별 문형이 각자 충족하는 시점에만 그 문형 하나를
  `_EXTRA_CHECKS`에서 빼고 판정에 맡긴다. 이 판단은 사람이 라벨링된 데이터를 보고
  수동으로 결정하며, 자동 전환 코드는 만들지 않는다.
- **일본어 감성 극성 사전을 3번째 심판으로 도입하지 않는다** — 검토 결과 결과
  강제 검증군 5개 중 3개(いかんによって·にかたくない·を禁じ得ない)는 극성이 아니라
  결합 규칙·중립 어휘 문제라 사전이 원리적으로 무관하고, 나머지 2개(あげく·ばかりに)도
  실제 사고 원인(体調を崩す 같은 구 단위 표현)에는 사전이 약해 실효성이 낮다.
- **날짜는 `_send_date()`로만 구한다** — `datetime.date.today()`도,
  `_today_kst()`도 직접 쓰지 않는다. GitHub은 예약 실행을 몇 시간씩 미루므로
  (실측 2시간52분~5시간41분) 실행 시각의 벽시계는 회차 날짜와 다를 수 있다.
  KST 벽시계를 쓰면 지연이 자정을 넘긴 순간 회차가 하루 밀리고, 금요일이면
  주말 가드에 걸려 발송이 사라진다(2026-09-14 월요일 회차가 화요일로 밀린 것이
  실제 사례). `_send_date()`는 정규 실행에 UTC 날짜를, 수동 실행에 KST 날짜를
  쓴다 — 자세한 근거는 함수 주석에 있다.
- **존재 판정 보정(`_PRESENCE_CHECKS`)을 문형에 일괄 적용하지 않는다** — `らしい`는
  Janome가 문형(`助動詞`)과 어휘(`素晴らしい` 등 `形容詞`)를 정확히 갈라주지만,
  `っぽい`는 정상 용법(`言っているっぽい`)까지 `形容詞`로 분석돼서 같은 규칙을 걸면
  문형 자체가 죽는다. 오탐이 실제로 관측된 문형만 하나씩 등록한다.
- **`_PRESENCE_CHECKS` 실패는 "문형 누락"으로 보고한다** — `_EXTRA_CHECKS`에 넣으면
  "구조 조건 미충족"으로 나가는데, 실제 상황은 그 문형을 아예 안 쓴 것이라 3장의
  사유 해석표와 어긋난다. 두 딕셔너리를 합치지 않는다.
- **쿨다운 우회가 `exclude_ids`까지 버리지 않게 한다** — `select_patterns()`의
  "후보 부족" 경로는 쿨다운만 풀고 직전 실패 조합은 계속 제외해야 한다. 재시도
  상황은 정의상 후보가 얇아진 상태라 이 우회가 반드시 발동하고, 전체 풀로
  되돌리면 재선정이 가장 필요한 순간에만 골라서 무력화된다(2026-09-15 인용
  회차에서 재선정 조합이 직전과 3/5 겹쳐 `そうだ(様態)`가 유임됐다).
- **`get_grammar.py`에 `os.environ`·`datetime.now`·`random`·`smtplib`·
  `subprocess`를 직접 쓰지 않는다** — 필요한 기능은 `ports.py`에 포트로
  선언하고 `adapters.py`에 구현한 뒤 `deps`로 주입받는다(README "왜
  의존성을 주입하는가" 참고). 예전에는 실행 로그를 읽거나 함수 본문을
  정규식으로 떼어내 `exec`해야만 동작을 확인할 수 있었고, 그 과정에서
  `exclude_ids`가 쿨다운 우회 경로에서 버려지는 걸 한 번 놓쳤다. `deps`
  없이 순수하게 텍스트만 변환하는 함수(`_check_*` 계열 등)까지 억지로
  주입 형태로 바꾸지는 않는다 — 부작용이 있는 함수에만 `deps`를 붙여서,
  시그니처 자체가 "이 함수는 바깥 세계를 건드린다"는 신호가 되게 한다.
- **`_EXTRA_CHECKS`의 어휘 목록 체크는 `_contains_any()`를 거쳐야 한다** —
  `any(w in window for w in WORDS)`로 직접 비교하지 않는다. Gemini는
  어휘를 사전형이 아니라 활용해서 쓰므로(2026-09-17 `落ち込んだ`가 목록의
  `落ち込む`와 안 맞아 `ばかりに` 정상 사용을 놓침), `_contains_any()`가
  원문과 Janome `base_form` 복원 텍스트를 모두 본다. 단, `Pattern.found_in()`
  기본 매칭과 `_TESHIMAU_RE`·`_check_rashii`는 여기 포함되지 않는다 —
  `しまう`(치우다/보조동사)처럼 활용형만으로 안 갈리는 동형이의어가 있어
  품사까지 봐야 하고, 파급 범위도 "89개 문형 전부의 판정 기준"으로
  훨씬 커진다(README "어휘 목록 매칭의 활용형 대응" 참고). 이쪽을 넓히는
  건 별도로 검토한다.
- **サ変동사(`察する` 등)를 콜로케이트 목록에 넣을 때 Janome의 `base_form`이
  관용적 사전형과 다를 수 있음을 먼저 확인한다** — `察する`는 `base_form`이
  `察す`로 나온다(る 없이). 목록 표기와 실제 `base_form`이 어긋나면
  `_contains_any()`의 활용형 매칭이 무력화된다. 아직 실관측된 실패가 없어
  예방적으로 고치지 않았다.

---

## 5. 작업 지시서 작성 원칙

이 리포에 대한 작업 지시서(claude.ai 대화에서 Claude Code로 전달하는 문서)는
토큰을 최소화하는 방향으로 작성한다.

- 배경은 한두 줄 요약으로 끝낸다. 이미 나눈 대화 내용을 다시 풀어 쓰지 않는다
- 코드는 변경되는 부분만 제시한다. 새 함수를 통째로 재기재하지 않는다 —
  Claude Code는 실물 파일을 직접 읽을 수 있으므로 전체 코드를 미리 줄 필요가 없다
- 체크리스트·검증 절차는 핵심 1~2개만 남긴다
- 소규모 변경(상수 값 조정 등)에는 매 작업 승인을 요구하지 않는다. "반영 후 diff
  한 번에 제시, 승인 기다리지 말고 진행"을 기본으로 한다. 여러 작업이 얽힌 큰
  변경(예: 자연스러움 판정 시스템 도입)에만 단계별 승인을 명시한다

---

## 6. 파일 위치 요약

| 파일 | 역할 |
|---|---|
| `grammar_bank.py` | 문형 뱅크, 카테고리, 위험 표현 지침 |
| `ports.py` | 주입 대상 정의(Clock·Rng·Llm·Mailer·Store·Vcs·Fonts, Deps 컨테이너) |
| `adapters.py` | 포트의 실제 구현 — 네트워크·SMTP·git·파일시스템은 여기서만 |
| `get_grammar.py` | 생성·검증·발송·실패 처리 전체 파이프라인 (Deps를 주입받음) |
| `tests/fakes.py` | 포트의 가짜 구현 — 테스트에서 네트워크 없이 파이프라인을 돌리기 위함 |
| `tests/test_pipeline.py` | `python -m unittest discover -s tests`로 실행 |
| `used_history.json` | 쿨다운용 사용 이력 (성공한 날만 갱신) |
| `failure_history.json` | 실패 이력 (실패한 날만 갱신, 반복 경고 판정에 사용) |
| `run_log.txt` | 매 실행의 상세 로그 (Actions 아티팩트로도 다운로드 가능) |
| `README.md` | 설계 배경, 왜 이렇게 만들었는지에 대한 상세 기록 |
| `MANUAL.md` | 이 문서 — 문제 발생 시 즉시 참고할 실전 절차 |
