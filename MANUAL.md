# japanese-study-grammar 운영 매뉴얼

이 문서는 "무슨 일이 있었는가"가 아니라 **"문제가 생기면 지금부터 뭘 해야 하는가"**를
정리한 실전 매뉴얼이다. 설계 배경이나 왜 이렇게 만들었는지는 `README.md`를 본다.

---

## 1. 이번 개선으로 달라진 것

### 이전 (수동 대응)

```
발송 실패 → 사용자가 GitHub Actions 접속 → 로그 펼치기 → 원인 찾기
→ Claude에게 캡처해서 전달 → 진단·수정 → 파일 다운로드 → 웹 업로드
```

### 지금 (자동 진단 + API 직접 커밋)

```
발송 실패 → 실패 알림 메일 하나로 원인 진단에 필요한 정보가 다 옴
→ 그 메일 내용을 Claude에게 그대로 전달 → 진단·수정
→ Claude Code 세션에서 수정 후 PR 생성
```

### 구체적으로 무엇이 바뀌었나

| 항목 | 이전 | 지금 |
|---|---|---|
| 실패 표시 | 조용히 종료(초록 체크로 위장) | `exit(1)`로 워크플로가 빨간 X로 표시됨 |
| 실패 원인 확인 | GitHub Actions 로그를 직접 열어야 함 | 실패 메일 본문에 시도별 진단이 다 담김 |
| 반복 문제 인지 | 매번 새로 판단 | 최근 5회 중 3회 이상 실패한 문형은 메일에 자동 경고 |
| 코드 반영 | 파일 다운로드 → 웹 업로드 | Claude Code 세션에서 수정 후 PR |
| 문장 수 미달/초과 | 검증 범위가 느슨해서 8~9문장도 통과된 적 있음 | 정확히 10~15문장만 통과 + 프롬프트에 목표치·자체 점검 지시 강화 |
| そうだ(伝聞/様態) 구분 | 문자열만 보고 구분 못 함 | Janome 형태소 분석으로 활용형까지 확인 |
| 위험 표현(あげく 등 12개) | 검증 없음 | 8개는 구조 검증, 나머지는 프롬프트 지침만(한계로 남음) |

---

## 2. 문제 발생 시 대응 절차

### 2-1. 실패 메일을 받았을 때 (가장 흔한 경우)

1. `hexartrion@gmail.com`에서 제목이 `[운영 알림] 표현독해 발송 실패`인 메일을 연다
2. 메일 본문 전체를 복사한다 (반복 경고, 시도별 진단 다 포함되어 있음)
3. Claude와의 대화에서 그대로 붙여넣고 "확인 및 검토" 요청
4. Claude가 원인을 진단하고 수정 방향을 제안 — **여기서 반드시 승인 여부를 확인받는다**
   (한 번에 다 고치려 하지 말고, 원인이 여러 개면 하나씩 검증 후 진행)
5. 승인하면 Claude가 로컬에서 코드 수정 → 컴파일 확인 → 모의 테스트로 검증
6. Claude Code 세션에서 커밋 후 PR을 생성한다
7. GitHub Actions에서 **Run workflow**로 같은 카테고리를 강제 지정해 재실행 확인

### 2-2. 지문 내용이 이상하다고 느껴질 때 (실패는 아니지만 품질 문제)

발송은 됐지만 문장이 어색하거나 문법이 의심스러운 경우:

1. 받은 지문 원문을 그대로 Claude에게 붙여넣고 "이 문장을 읽고 검수" 요청
2. Claude가 문법적 정확성과 자연스러움을 검토
3. 실제 결함이 확인되면, **해당 문형 하나에 한해서만** 지침 보강 여부를 논의
   (89개 전체에 미리 지침을 도배하지 않는다 — 부작용이 더 크다는 게 이미 확인됨)
4. 수정 후 Claude Code 세션에서 PR 생성, Actions에서 같은 카테고리로 재실행해 확인

### 2-3. 워크플로 자체가 몇 주간 계속 실패할 때 (구조적 문제 의심)

1. `failure_history.json`을 GitHub에서 직접 열어 최근 실패 패턴을 확인
2. 특정 카테고리(예: N1문어체)에서만 반복된다면, 그 카테고리의 문형 조합 자체를 재검토
3. `PATTERNS_PER_DAY`(현재 5)를 줄이는 것도 검토 대상 — README의 "문형 조합 충돌 방지"
   섹션 참고

---

## 3. 자주 나오는 실패 사유와 1차 해석

| 실패 사유 문구 | 의미 | 우선 확인할 것 |
|---|---|---|
| `문장 수 N개 — 범위(10~15) 벗어남` | Gemini가 지시한 분량을 못 지킴 | 반복되면 프롬프트 목표치 재조정 검토 |
| `문형 누락: XX` | Gemini가 지정한 문형을 아예 안 씀 | 문형 조합 자체가 어려운 조합인지 확인 |
| `구조 조건 미충족: XX` | 위험 문형 12개 중 하나가 잘못 쓰임 | 실제 지문에서 어떻게 잘못 썼는지 원문 확인 후 그 문형 하나만 지침 보강 |
| `Gemini 출력 파싱 실패` | `---` 구분자를 안 지킴 | Gemini API 자체 이슈일 수 있음, 며칠 지켜보고 반복되면 프롬프트 포맷 지시 강화 |

---

## 4. 손대면 안 되는 것 (이미 검증된 함정)

- **89개 문형에 지침을 한꺼번에 추가하지 않는다** — N1문어체의 격언체 문형(べからず 등)에는
  오히려 역효과. 실제 문제가 확인된 문형 하나씩만 개별 대응한다.
- **검증 창(window) 크기를 무작정 넓히지 않는다** — `ない`처럼 흔한 단어 기반 검증은
  창을 넓히면 검증 자체가 무의미해진다.
- **결과 강제 문형(あげく·ばかりに·ないことには·なくしては·いかんによって) 5개는 하루
  최대 1개만 뽑히도록 고정되어 있다** — 이 로직을 건드리면 문장 수 실패율이 다시
  올라갈 수 있다(실제로 겪었던 문제).
- **Claude API나 유료 AI로 문법 재검증하는 방식은 이미 검토 후 보류했다** — 무료 운영
  전제와 안 맞는다. 다시 검토하려면 이 전제 자체를 먼저 재논의해야 한다.
- **`_normalize()`의 치환 순서를 바꾸지 않는다** — `故に`→`ゆえに` 정규식은 반드시
  `_KANJI_TO_KANA`(事→こと 포함) 치환보다 먼저 실행해야 한다. 순서를 바꾸면 `事故に`/
  `縁故に`의 `事`/`縁`이 먼저 `こと`/그대로 남은 상태에서 정규식이 돌지 않고, `事`가
  `こと`로 먼저 치환되어 룩비하인드가 무력화되면서 `事故に`가 `ゆえに`로 오탐 치환된다.
- **정규화 테이블에 어휘를 추가할 때 오탐 범위를 먼저 확인한다** — `故` 한 글자처럼 흔한
  글자를 무작정 치환 대상에 넣으면 `事故`·`縁故`·`故郷` 같은 무관한 단어 안에서도 우연히
  매치될 수 있다. 추가 전에 어떤 기존 단어와 겹치는지 확인하고, 필요하면 정규식
  룩비하인드로 범위를 제한한다.

---

## 5. 파일 위치 요약

| 파일 | 역할 |
|---|---|
| `grammar_bank.py` | 문형 뱅크, 카테고리, 위험 표현 지침 |
| `get_grammar.py` | 생성·검증·발송·실패 처리 전체 파이프라인 |
| `used_history.json` | 쿨다운용 사용 이력 (성공한 날만 갱신) |
| `failure_history.json` | 실패 이력 (실패한 날만 갱신, 반복 경고 판정에 사용) |
| `run_log.txt` | 매 실행의 상세 로그 (Actions 아티팩트로도 다운로드 가능) |
| `README.md` | 설계 배경, 왜 이렇게 만들었는지에 대한 상세 기록 |
| `MANUAL.md` | 이 문서 — 문제 발생 시 즉시 참고할 실전 절차 |
